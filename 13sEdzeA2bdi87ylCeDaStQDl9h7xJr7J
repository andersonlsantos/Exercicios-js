
/**
 * 08) ​ Pedro joga N jogos de basquete por temporada. Para saber como ele está progredindo, ele mantém
        registro de todas pontuações feitas por jogo. Após cada jogo ele anota o valor e confere se o
        mesmo é maior ou menor que seu melhor e pior desempenho. Dada uma lista string = “pontuação1 pontuação2
        pontuação3 etc..”, escreva uma função que ao recebê-la irá comparar os valores um a um e irá retornar um
        vetor com o número de vezes que ele bateu seu recorde de maior número de pontos e quando fez seu pior
        jogo. (Número do pior jogo).Obs.: O primeiro jogo não conta como novo recorde do melhor.
        Exemplo:
        String: “10 20 20 8 25 3 0 30 1”
        Retorno: [3, 7] (Significa que ele bateu três vezes seu recorde de melhor pontuação e a pior pontuação
        aconteceu no sétimo jogo.)
 */


const jogos = (lista) => {
    let cont = 0
    let cont2 = 0
    let maior = 0
    let menor = 1000000000000
    lista.split(' ').forEach((valor, indice) => {

        //console.log(indice, valor)
        
        if (parseInt(valor) > maior) {
            maior = valor
            cont += 1
        }

        if(parseInt(valor) < menor){
            menor = valor
            cont2 = indice
        }

    })
    console.log(`${cont - 1}, ${cont2 + 1}`)
}

jogos('8 9 3 4 5 2 1 10 13 20 14 25 30')
